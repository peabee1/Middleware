/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special configuration required for an MCP-only API server.
  // Add framework options here as needs emerge.
};

module.exports = nextConfig;
